#include "stdafx.h"
#include <iostream>
#include "conio.h"

using namespace std;
void main()
{
	int n,num=0,i,sum=0;
	cout<<"ingresar numeros pares a sumar: "<<endl;
	cin>> n;
	for(i=0;i<n;i++);
	{num=num+2;
	sum=sum+num;
	}
	cout<<"la suma es; "<<sum;
	getch();
}

